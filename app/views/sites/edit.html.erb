<% content_for :title, "現場編集 - #{@site.name}" %>
<div class="mb-4">
  <%= link_to "← #{@site.name}", @site, class: "text-decoration-none text-muted small" %>
</div>
<h1 class="h3 mb-4">現場編集</h1>
<div class="card shadow-sm" style="max-width: 640px;">
  <div class="card-body p-4">
    <%= render 'form' %>
  </div>
</div>
